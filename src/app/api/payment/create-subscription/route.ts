// TODO: Create subscription route handler. This will be called from the frontend when a user wants to start a subscription.
export async function POST() {
  // import { startSubscription } from '@/services/billing.service';
  //   const data = await startSubscription(firebase_uid);
  //   return Response.json(data);
}
