echo "Running commit message checks..."

pnpm exec commitlint --edit "${1}"

if [ $? -ne 0 ]; then
echo "Commit message checks failed. Please fix the issues before committing."
exit 1
fi

echo "Commit message checks passed. Proceeding with commit."
